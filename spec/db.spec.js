'use strict';

// var chai, db, crawler, _;

// chai = require('chai');
// db = require('../database');
// crawler = require('../crawler');
// _ = require('lodash');
// chai.should();

// var url = 'https://news.ycombinator.com/';
// // runs before all tests in this block
// var topics = crawler.firstPage(url);
// _.each(topics, function (topic) {
//   db.addToIndex(topic.href, topic.text, topic.score);
// });

// describe('Database module', function () {
//   var topics;

//   before(function (done) {

//     done();
//   });

//   // it ('should return true', function () {
//   //   var url = 'https://github.com/joushou/serve2d';
//   //   db.isIndexed(url).should.be.true;
//   // });

//   it ('should return something', function (done) {

//     // _.each(topics, function (topic) {

//     // });
//     done();
//   });
// });
